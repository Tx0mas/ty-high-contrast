# ty-high-contrast

Este es el theme que uso y para tenerlo en cualquier lado lo hice un plugin..

# Lazy:

```
return
{
    "Tx0mas/ty-high-contrast",
    lazy = false, 
    priority = 1000, 
    config = function()
    end,
}
```
